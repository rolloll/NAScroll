<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".NasViewerApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.NasViewer"
        android:supportsRtl="false">

        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.NasViewer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".BrowserActivity"
            android:exported="false" />

        <activity
            android:name=".ReaderActivity"
            android:exported="false"
            android:theme="@style/Theme.NasViewer.Reader" />

        <activity
            android:name=".ReadingNotesActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title" />

        <activity
            android:name=".TextReaderActivity"
            android:exported="false" />

        <activity
            android:name=".EpubReaderActivity"
            android:exported="false"
            android:theme="@style/Theme.NasViewer.Reader" />

        <activity
            android:name=".TocActivity"
            android:exported="false" />

        <activity
            android:name=".PdfReaderActivity"
            android:exported="false"
            android:theme="@style/Theme.NasViewer.Reader" />

    </application>
</manifest>
